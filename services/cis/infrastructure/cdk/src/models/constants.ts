const API_PREFIX = 'contract-api';

const BY_CONTRACT_DETAILS_IDENTITY_GSI_NAME = 'contract_details_identity_index';
const LIVE_ALIAS_NAME = 'live';
const MAX_PROVISIONED_CAPACITY_FOR_LAMBDA = 500;
const constants = {
    API_PREFIX,
    BY_CONTRACT_DETAILS_IDENTITY_GSI_NAME,
    LIVE_ALIAS_NAME,
    MAX_PROVISIONED_CAPACITY_FOR_LAMBDA,
};

export { constants };
